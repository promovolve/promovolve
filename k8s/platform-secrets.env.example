JWT_SECRET=generate-with-python3-secrets.token_urlsafe-48

# Optional but recommended — must MATCH the api side's INTERNAL_API_KEY
# (secrets.env.example). Sent as X-Internal-Key by the billing settlement
# job when calling the core's /v1/internal endpoints.
INTERNAL_API_KEY=
